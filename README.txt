"CIM_RECG_DATAS_W.txt"是原始数据文件。

"CIM_RECG_DATAS_W_别动__proc.txt"(总共10000行，ECG采样2000Hz，第一列是时间，单位秒，其他列是12导联数据)是经过中值滤
    波算法(窗口大小2000)过滤过的数据，是只读的。

这两个文件是干嘛用的：当你要写一个新的中值滤波算法的时候，可以用这两个文件来验证你的算法的执行结果是不是正确的。